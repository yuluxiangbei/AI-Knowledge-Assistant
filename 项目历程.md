# 项目历程(学习回顾)

> 记录从零开发 AI-Knowledge-Assistant 的每一步:知识点、踩过的坑、验证方式。
> 更新频率:每完成一课更新一次。

---

## 第十课:Vue 3 前端(2026-08-04~08-05,已完成)

### 背景:为什么换掉 Streamlit

- 原计划第十课用 Streamlit 做前端,已写了登录/注册表单(selectbox 切换 + session_state 存 token)
- **踩到 Streamlit 会话大坑**:登录成功后刷新页面 → session_state 丢失、会话 ID 变化
- 排查链(重要经验):代码逻辑 ✓ → 后端 ✓ → 代理 ✓ → cookie 域 ✓ → 最终用 CDP 自动化浏览器+最小复现页确认:**Streamlit 1.59.2/1.60.0 在 Windows+Python 3.13+Edge 环境下,刷新后 session_state 不保留**(count 点按钮 1→4,刷新回到 1)
- 结论:Streamlit 会话恢复依赖 WebSocket 握手头传 existing_session_id,前端 JS 存在内存,刷新即失;该环境无兜底
- 尝试过:固定 cookieSecret + serverAddress(localhost)、清 cookie、换 IP/127.0.0.1、st.query_params 兜底(可用但体验一般)
- 用户决定:**换 Vue 3 + Vite 实现前端**(后端 API 全部就绪,只换壳,顺便做出前后端分离架构)

### 成果(已完成)

- `npm create vite@latest frontend-vue -- --template vue` 脚手架 + `npm install` 完成,Vite dev server 跑在 5173
- 删掉了 Streamlit 4 个实例(8501-8504 已全部关闭)
- `src/App.vue` + `src/components/AI-Knowledge.vue`:组件化链路跑通,页面显示 "AI智能知识助手" 标题
- 学到:**组件不是自己出现的**——App.vue import 组件并在 template 里用 `<AIKnowledge />` 才会渲染
- 组件命名踩坑:变量名不能有连字符(`import AI-Knowledge` 报错 `expected from`),要驼峰/下划线

### CORS 配置(任务 3 ✅)

- **CORS 是必过的一关**:Vue(5173)→ 后端(8000)跨域,浏览器会拦截
- 发现:后端 `app/main.py` **根本没有 CORS 中间件**!config.py 里 `CORS_ORIGINS` 定义了但没人用(所以 Streamlit 版没报跨域错——它是 Python 进程发请求,不是浏览器)
- 已解决:① main.py 加 CORSMiddleware(`allow_origins=get_settings().CORS_ORIGINS` + allow_credentials + 通配 methods/headers) ② .env 的 CORS_ORIGINS 加 http://localhost:5173 ③ 重启后端(注意 .env 变更不会自动 reload)

### 前端功能(任务 4~5 ✅,全部在单文件 `frontend-vue/src/components/AI-Knowledge.vue`)

- **登录/注册**:axios 发 `/api/v1/auth/*`,成功把 token 存 `localStorage`(刷新不丢,这正是换掉 Streamlit 的痛点)
- **请求带凭证**:每个请求头 `Authorization: Bearer ${token}`,token 从 localStorage 初始化(注意:未登录时后端会拦,前端 catch 里显示 `detail`)
- **上传文档**:`FormData` + `POST /api/v1/documents/upload`;文档列表加载 + 删除(后端联动删向量)
- **会话管理**:新建/列表/选择/删除会话,侧边栏 ChatGPT 风格布局
- **聊天问答**:`POST /api/v1/conversations/{id}/messages`,乐观更新(先 push 用户消息再等 AI 返回),AI 回答带 `sources` 引用
- **引用溯源展示**:assistant 消息带 `sources` 时渲染"引用来源"标签(sources-title + source-tag)
- **交互细节**:Enter 发送 / Shift+Enter 换行;发送中 `sending` 置位防重复;错误提示 `出错了: <detail>`

### 验证方式

- 启动后端(命令见速查)后,浏览器访问 `http://localhost:5173`,注册 → 登录 → 上传文档 → 新建会话提问,确认带引用来源的回答能展示
- 重启后端时注意:.env 变更不会触发 reload,必须手动重启

### 面试知识点(本课已沉淀)

1. **Streamlit session 机制**:session id 靠前端 JS 内存存,刷新即失;cookieSecret 不固定时重启全部失效
2. **前后端分离的 CORS**:浏览器同源策略;Streamlit 进程内发请求无跨域,Vue 浏览器发请求必遇 CORS;后端用 CORSMiddleware 配置 allow_origins/allow_credentials/allow_methods/allow_headers
3. **Vue 组件化**:单文件组件 = template + script + style;组件要 import + 注册才能用;命名规范驼峰
4. **localStorage 持久化 token**:刷新不丢(选 Vue 的关键原因);对比 sessionStorage(关标签页丢)、JS 内存(刷新即丢)——和 Streamlit session_state 的痛点形成对照
5. **乐观更新(optimistic update)**:先本地渲染用户消息再发请求,响应后追加 AI 回答,失败再提示;交互跟手,但要注意 `sending` 置位防重复提交

---

## 环境准备

- **Python 环境**:conda 环境 `AI-Knowledge`(Python 3.13.9),路径 `D:\Anaconda\envs\AI-Knowledge\python.exe`
- **数据库**:MySQL 8.4 跑在 Docker 容器 `ai-knowledge-mysql`(端口 3306,库 `ai_knowledge`,账号 `app/app123456`,root 密码 `root123456`)
- **远程仓库**:GitHub `yuluxiangbei/AI-Knowledge-Assistant`(SSH key:`~/.ssh/github_ed25519`)
- **重要**:conda 的 `activate` 在 PowerShell 里需要先 `conda init powershell` 才能生效;不生效时用全路径 `D:\Anaconda\envs\AI-Knowledge\python.exe` 直接调用

---

## 第一课:FastAPI 最小应用(2026-08-01)

### 成果
`app/main.py` — FastAPI 实例 + `GET /health` 返回 `{"status":"ok"}`

```python
from fastapi import FastAPI

app = FastAPI()

@app.get("/health")
def get_health():
  return {"status":"ok"}
```

### 知识点
1. FastAPI 实例:`app = FastAPI()`
2. 路由装饰器:`@app.get("/health")`
3. 返回字典自动转 JSON
4. 启动命令:`python -m uvicorn app.main:app --reload`
   - `app.main` = app 包里的 main 模块
   - `app`(冒号后)= 模块里的应用实例名
5. **文件扩展名必须是 `.py`**,否则 Python 不认它是模块

### 踩过的坑
- 文件名写成 `main` 没有 `.py` 扩展名 → uvicorn 无法启动
- 端口被占报 `WinError 10013/10048` → 检查 `Get-NetTCPConnection -LocalPort 8000` 找到占用进程杀掉

### 验证
浏览器访问 `http://127.0.0.1:8000/health` → `{"status":"ok"}`

---

## 第二课:项目分层 + 配置管理(2026-08-01)

### 成果
- 目录结构:`app/core`(配置/安全)、`app/db`(数据库)、`app/api/routers`(接口)、`app/schemas`(校验)、`app/models`(表模型)、`app/services`(业务)
- 每个目录放 `__init__.py` 让 Python 识别为包
- `app/core/config.py` — pydantic-settings 配置管理

```python
from functools import lru_cache

from pydantic_settings import BaseSettings, SettingsConfigDict


class Settings(BaseSettings):
    model_config = SettingsConfigDict(env_file=".env", env_file_encoding="utf-8")

    APP_NAME: str = "AI-Knowledge-Assistant"
    SECRET_KEY: *** = "change-me"
    DATABASE_URL: str = "mysql+asyncmy://app:app123456@127.0.0.1:3306/ai_knowledge?charset=utf8mb4"
    # ... 其他配置项


@lru_cache
def get_settings() -> Settings:
    return Settings()
```

### 知识点
1. **config.py 解决什么问题**:把"会变的东西"(密钥、数据库地址)从代码里抽出来。代码只写默认值,真实值放 `.env`。`.env` 有值 → 覆盖默认值;没有 → 用默认值
2. 好处:换环境只改 `.env` 不改代码;密钥不进 git 不泄露;配置集中管理
3. `SettingsConfigDict(env_file=".env")`:告诉 pydantic 去 .env 读取
4. `@lru_cache`:配置对象只创建一次,以后调用走缓存
5. 类型注解很重要:`int` 类型会自动把 `.env` 的字符串转成整数

### 踩过的坑
- `.env` 里有的变量,`Settings` 类里没定义 → 报 `Extra inputs are not permitted`。解决:把配置项全部声明到类里(规范做法),或在 `SettingsConfigDict` 加 `extra="ignore"`(偷懒做法)
- 导入顺序规范:标准库(`functools`)在最上,第三方库在下面(风格问题,不影响运行)

### 验证
```powershell
python -c "from app.core.config import get_settings; s = get_settings(); print(s.APP_NAME)"
```

---

## 第三课:数据库连接(2026-08-01)

### 成果
`app/db/session.py` — 异步引擎 + 会话工厂 + FastAPI 依赖

```python
from collections.abc import AsyncGenerator

from sqlalchemy.ext.asyncio import AsyncSession, async_sessionmaker, create_async_engine

from app.core.config import get_settings

settings = get_settings()

engine = create_async_engine(
  settings.DATABASE_URL,   # 从配置读连接串
  echo = False,            # True 会打印所有 SQL(调试用)
  pool_pre_ping = True,    # 借连接前先 ping,防止用上死连接
)

AsyncSessionLocal = async_sessionmaker(engine, expire_on_commit=False)

async def get_db() -> AsyncGenerator[AsyncSession, None]:
  async with AsyncSessionLocal() as session:
    yield session
```

### 知识点(本课最核心)
1. **三层结构**:
   - `engine` = 连接池管家,应用启动建一次,全局共用,绝不每个请求重建
   - `AsyncSessionLocal` = 会话工厂,每次要"对话"时拿一个会话
   - `get_db()` = FastAPI 依赖,请求来 → 开会话 → 给路由用 → 结束自动关
2. **`async with`**:进入时自动初始化,退出时自动关闭(即使中途报错也会关)。防连接泄漏的保险
3. **`yield` vs `return`**:
   - `return` = 交完就死,函数结束,收尾代码永远不执行 → 连接泄漏
   - `yield` = 交完待命,函数暂停但活着,路由用完唤醒继续收尾
   - get_db 用 yield 是因为**要保证事后关闭会话**
4. **`expire_on_commit=False`**:commit 后对象属性不过期,不用重新查库
5. **`pool_pre_ping=True`**:MySQL 连接闲置久了会断,借出前先试探,防"死连接"灵异 bug
6. **async + yield 组合**:`async def` 管 await(异步操作),`yield` 管暂停交出。只有 async 没 yield 跑完就结束没人收尾;只有 yield 没 async 没法 await 异步关闭。两者都要 → 异步生成器

### 踩过的坑
- `async_sessionmaker` 参数名拼错:`engine_on_commit` → 正确是 `expire_on_commit`(拼写错误用 IDE 提示避免)
- PowerShell 命令行写多行 Python 引号冲突 → 改用脚本文件 `test_db.py`

### 验证
```python
# test_db.py
import asyncio
from sqlalchemy import text
from app.db.session import engine

async def main():
    async with engine.connect() as conn:
        result = await conn.execute(text("SELECT 1"))
        print("数据库连接成功:", result.scalar())
    await engine.dispose()

asyncio.run(main())
```

---

## 第四课:ORM 模型 + 建表(2026-08-01)

### 成果
- `app/db/base.py` — 所有模型的基类

```python
from sqlalchemy.orm import DeclarativeBase

class Base(DeclarativeBase):
  pass
```

- `app/models/user.py` — User 模型

```python
from sqlalchemy import String
from sqlalchemy.orm import Mapped, mapped_column

from app.db.base import Base

class User(Base):
  __tablename__ = "users"

  id: Mapped[int] = mapped_column(primary_key=True, autoincrement=True)
  username: Mapped[str] = mapped_column(String(64), unique=True, nullable=False)
  hashed_password: Mapped[str] = mapped_column(String(255), nullable=False)
```

- `app/models/__init__.py` — 汇总导出

```python
from app.models.user import User
```

- `app/init_db.py` — 建表脚本

```python
import asyncio

from app import models  # noqa: F401  确保模型被导入注册
from app.db.base import Base
from app.db.session import engine


async def init():
    async with engine.begin() as conn:
        await conn.run_sync(Base.metadata.create_all)


if __name__ == "__main__":
    asyncio.run(init())
```

### 知识点
1. **ORM = 对象关系映射**:用 Python 类描述表,SQLAlchemy 翻译成 SQL。好处:不手写 SQL、换库不改代码、查询结果是对象、防 SQL 注入
2. `Mapped[int]` / `Mapped[str]`:类型注解 → 列类型(int→INTEGER, str→VARCHAR)
3. `String(64)`:限定长度(对应 VARCHAR(64))。`hashed_password` 用 255 因为 bcrypt 哈希 60 字符,留余量
4. `unique=True` → 数据库唯一索引(UNI),并发下也不会插入重复数据
5. **`__init__.py` 汇总导出**:模型必须被导入过,SQLAlchemy 才知道它的存在。`from app import models` 只加载 `__init__.py`,不写导入行 → User 不会被注册 → `create_all` 无事可做
6. **模型与连接分离**:models 只依赖 Base 不碰 engine,避免循环依赖,可复用

### 踩过的坑
1. `python app/init_db.py` 报 `ModuleNotFoundError: No module named 'app'`:
   - `python file.py` = 把文件所在目录当工作目录 → 找不到 app 包
   - `python -m app.init_db` = 从当前目录找 → 找得到
   - 项目脚本统一用 `-m` 方式(uvicorn 也是)
2. 数据库环境残留:推倒重来后,MySQL 里还有旧表。`create_all` 发现表存在就跳过,不会重建
   - 清理:`docker exec ai-knowledge-mysql mysql -uroot -proot123456 -e "DROP DATABASE ai_knowledge; CREATE DATABASE ai_knowledge;"`
   - 教训:**代码推倒重来 ≠ 数据库自动清空**
3. 建表后 `SHOW TABLES` 为空 → 排查出是 `__init__.py` 没导入模型

### 验证
```powershell
python -m app.init_db
docker exec ai-knowledge-mysql mysql -uapp -papp123456 ai_knowledge -e "SHOW TABLES;"
docker exec ai-knowledge-mysql mysql -uapp -papp123456 ai_knowledge -e "DESC users;"
```

最终表结构:
| Field | Type | Null | Key | Default | Extra |
|---|---|---|---|---|---|
| id | int | NO | PRI | NULL | auto_increment |
| username | varchar(64) | NO | UNI | NULL | |
| hashed_password | varchar(255) | NO | | NULL | |

---

## 第五课:认证接口(注册/登录)(2026-08-01)

### 成果
- `app/core/security.py` — bcrypt 密码哈希 + JWT 签发/验证
- `app/schemas/auth.py` — 请求/响应模型
- `app/api/deps.py` — DbDep 数据库依赖
- `app/api/routers/auth.py` — 注册/登录路由
- `app/api/routers/__init__.py` + `app/main.py` — 路由挂载

#### security.py(密码哈希)

```python
import bcrypt


def hash_password(plain: str) -> str:
  passwd = bcrypt.hashpw(plain.encode("utf-8"), bcrypt.gensalt())
  return passwd.decode()


def verify_password(plain: str, hashed: str) -> bool:
  result = bcrypt.checkpw(plain.encode("utf-8"), hashed.encode("utf-8"))
  return result
```

#### security.py(JWT)

```python
from datetime import datetime, timedelta, timezone
import jwt
from app.core.config import get_settings

settings = get_settings()


def create_access_token(subject: str | int) -> str:
  exp = datetime.now(timezone.utc) + timedelta(minutes=settings.ACCESS_TOKEN_EXPIRE_MINUTES)
  payload = {"sub": str(subject), "exp": exp}
  return jwt.encode(payload, settings.SECRET_KEY, algorithm=settings.ALGORITHM)


def decode_access_token(token: str) -> dict:
  return jwt.decode(token, settings.SECRET_KEY, algorithms=[settings.ALGORITHM])
```

#### auth.py 路由(核心逻辑)

```python
@router.post("/register", response_model=TokenOut, status_code=status.HTTP_201_CREATED)
async def register(payload: RegisterRequest, db: DbDep):
  existing = await db.scalar(select(User).where(User.username == payload.username))
  if existing:
    raise HTTPException(status_code=409, detail="用户名已存在")
  user = User(username=payload.username, hashed_password=hash_password(payload.password))
  db.add(user)
  await db.commit()
  await db.refresh(user)
  return TokenOut(access_token=create_access_token(user.id), user=UserOut.model_validate(user))


@router.post("/login", response_model=TokenOut)
async def login(payload: LoginRequest, db: DbDep):
  user = await db.scalar(select(User).where(User.username == payload.username))
  if not user or not verify_password(payload.password, user.hashed_password):
    raise HTTPException(status_code=401, detail="用户名或密码错误")
  return TokenOut(access_token=create_access_token(user.id), user=UserOut.model_validate(user))
```

### 知识点
1. **为什么密码不能明文存库**:数据库被拖库则全部泄露;很多人一个密码用所有网站,连累其他账号。解决方案:哈希(不可逆)+ 加盐(相同密码不同哈希,防彩虹表)。bcrypt = 加盐 + 故意算得慢(暴力破解成本高)
2. **bcrypt 哈希串结构**:`$2b$12$盐(22字符)哈希值` — 版本 + 计算成本(2^12)+ 盐 + 哈希
3. **盐藏在哈希里**:gensalt 随机 → 每次哈希结果不同;checkpw 从哈希中取出盐重算比对 → 验证总能成功
4. **JWT 三段结构**:头部(算法).载荷(数据).签名(防篡改)。签名 = 内容 + SECRET_KEY 的"印章"
5. **JWT 流程**:登录成功 → 发证;后续请求带证;服务器验签不查库(签名自证)
6. **sub 存字符串**(JWT 规范要求)、**exp 用 UTC**(服务器可能在任意时区)
7. **防篡改实测**:改 token 一个字符 → `InvalidSignatureError`
8. **密钥安全**:SECRET_KEY ≥32 字节、随机、藏 .env 不进 git(实测 9 字节触发 InsecureKeyLengthWarning)
9. **Pydantic 校验**:`Field(min_length=...)` 请求格式;`ConfigDict(from_attributes=True)` 支持 ORM→响应模型转换
10. **token_type="bearer"**:OAuth2 约定,告诉客户端令牌以 `Authorization: Bearer <token>` 携带
11. **安全细节**:UserOut 不暴露 hashed_password;用户不存在/密码错误统一 401(防用户名枚举);重复注册 409
12. **from_attributes 缺失后果**:返回 ORM 对象时 FastAPI 不知道去哪取值,直接报错

### 踩过的坑
1. **类名拼写错误**:`LoginReqquest` 多一个 q → 导入报 ImportError。避免:写完跑 `python -c "from app.schemas.auth import *"` 验证 + IDE 波浪线提示
2. **参数名与模型字段不一致**:`User(hash_password=...)` 应为 `User(hashed_password=...)` → TypeError
3. **登录逻辑反了**:`verify_password(明文, 新生成的哈希)` 恒为 True → 永远 401。正确:`not verify_password(明文, user.hashed_password)`
4. **tags 拼错** `"ayth"` → Swagger 分组名错
5. **python -c 长命令在 PowerShell 折行** → 改脚本文件(test_auth.py)
6. **运行目录不对** → `No module named 'app'`。所有 `python -m`/`from app...` 都在项目根目录跑
7. **502 + JSONDecodeError**:服务器未启动或 --reload 重载中请求 → 空响应。先确认 `8000 端口在监听` 再测
8. **mysql -e 命令被 PowerShell 折行** → `-e` 后的 SQL 要写在同一行

### 验证
```python
# test_auth.py(临时)
import asyncio
import httpx

async def main():
    async with httpx.AsyncClient() as client:
        r1 = await client.post("http://127.0.0.1:8000/api/v1/auth/register",
                               json={"username": "alice3", "password": "secret123"})
        print("注册状态码:", r1.status_code)      # 201
        print("注册响应:", r1.json())
        r2 = await client.post("http://127.0.0.1:8000/api/v1/auth/register",
                               json={"username": "alice3", "password": "secret123"})
        print("重复注册状态码:", r2.status_code)   # 409
        r3 = await client.post("http://127.0.0.1:8000/api/v1/auth/login",
                               json={"username": "alice3", "password": "secret123"})
        print("登录状态码:", r3.status_code)       # 200
        print("登录响应:", r3.json())

asyncio.run(main())
```

数据库验证(密码是哈希不是明文):
```sql
SELECT id, username, hashed_password FROM users;
-- 结果: $2b$12$... 开头,无明文
```

---

## 第六课:会话与消息模型 + 当前用户接口(2026-08-02)

### 成果
- `app/models/conversation.py` — 会话表(1 用户 → N 会话)
- `app/models/message.py` — 消息表(1 会话 → N 消息,级联删除)
- `app/api/deps.py` — 新增 `get_current_user` 依赖 + `CurrentUser` 别名
- `app/api/routers/users.py` — `GET /users/me` 当前用户接口
- `app/schemas/conversation.py` — 会话/消息请求响应模型
- `app/api/routers/conversations.py` — 6 个接口(创建/列表/详情/删除/发消息/看消息)
- `test_conversations.py` — 15 项全链路测试(含 3 项越权)

#### 会话模型(conversation.py)
```python
class Conversation(Base):
  __tablename__ = "conversations"
  id: Mapped[int] = mapped_column(primary_key=True, autoincrement=True)
  user_id: Mapped[int] = mapped_column(ForeignKey("users.id"), index=True, nullable=False)
  title: Mapped[str] = mapped_column(String(100), default="新对话", nullable=False)
  created_at: Mapped[datetime] = mapped_column(DateTime, server_default=func.now())
  updated_at: Mapped[datetime] = mapped_column(DateTime, server_default=func.now(), onupdate=func.now())
```

#### 消息模型(message.py)
```python
class Message(Base):
  __tablename__ = "messages"
  id: Mapped[int] = mapped_column(primary_key=True, autoincrement=True)
  conversation_id: Mapped[int] = mapped_column(
      ForeignKey("conversations.id", ondelete="CASCADE"), index=True, nullable=False)
  role: Mapped[str] = mapped_column(String(20), nullable=False)   # "user" / "assistant"
  content: Mapped[str] = mapped_column(Text, nullable=False)
  created_at: Mapped[datetime] = mapped_column(DateTime, server_default=func.now())
```

#### 依赖注入(get_current_user)
```python
oauth2_scheme = OAuth2PasswordBearer(tokenUrl="/api/v1/auth/login")

async def get_current_user(token: Annotated[str, Depends(oauth2_scheme)], db: DbDep) -> User:
  credentials_exception = HTTPException(
      status_code=401, detail="登录状态无效或已过期",
      headers={"WWW-Authenticate": "Bearer"})
  try:
    payload = decode_access_token(token)
    user_id = int(payload["sub"])
  except Exception:
    raise credentials_exception
  user = await db.get(User, user_id)
  if not user:
    raise credentials_exception
  return user

CurrentUser = Annotated[User, Depends(get_current_user)]
```

#### 归属校验模式(conversations.py)
```python
async def _get_owned_conversation(db: DbDep, conversation_id: int, user) -> Conversation:
  conv = await db.get(Conversation, conversation_id)
  if not conv or conv.user_id != user.id:
    raise HTTPException(status_code=404, detail="会话不存在")
  return conv
# 每个涉及具体会话的接口第一行都调它,校验不过自动 404
```

### 知识点
1. **一对多建模**:子表外键 `ForeignKey("users.id")` 指向父表主键;`index=True` 给外键列建索引(按用户查会话走索引)
2. **级联删除**:`ondelete="CASCADE"` 写在 ForeignKey 里——父行删除,子行自动删。验证要看 `SHOW CREATE TABLE`(DESC 看不到外键约束)
3. **两种默认值**:`default=` Python 侧(ORM 插入时生效,裸 SQL 不生效);`server_default=` 数据库侧(裸 SQL 也生效)。时间戳用 server_default;`onupdate=func.now()` 更新自动刷新
4. **SQLAlchemy 2.0 注解推断 nullable**:`Mapped[datetime]`(非 Optional)→ NOT NULL;想可空必须写 `Mapped[datetime | None]`
5. **依赖注入实战**:`OAuth2PasswordBearer` 从 `Authorization: Bearer <token>` 抠 token(tokenUrl 只给 Swagger 用);依赖函数可再依赖别的依赖,FastAPI 层层自动解析;失败统一 401 + `WWW-Authenticate: Bearer`
6. **db.get(模型, 主键)**:按主键查一行,查不到返回 None(等价 select + where id=)
7. **db.scalars 查多行** + `.all()` 取列表;`order_by(...desc())` 倒序(最新在前)
8. **归属校验**:资源属于当前用户才放行,否则 404 而不是 403(不暴露资源存在性,防枚举探测)
9. **信任边界**:role 由服务器写死 "user",客户端只传内容——防伪造"AI 消息"
10. **路径参数** `{conversation_id}` 自动解析成 int;`@router.post("")` 空路径 = 用 prefix 本身;204 删除无响应体,函数不 return

### 踩过的坑
1. **`Dserver_default` 拼错** → SAWarning + **静默忽略**:SQLAlchemy 把认不出的关键字当"方言参数",不报错只是警告 → created_at 没默认值。**Warning 不能无视**
2. **`onupdate="CASCADE"` 写错位置**:ondelete 必须写在 `ForeignKey(...)` 里;`mapped_column(onupdate=...)` 是"Python 侧更新默认值"(每次 UPDATE 把该列设为 'CASCADE' 字符串)→ 潜伏运行时炸弹
3. **await 丢失 → 越权漏洞(最危险)**:`if _get_owned_conversation(...)` 没 await → 协程对象永远为真 → 校验失效,能往别人的会话发消息/看别人的消息。**凡是调用 async 函数,先问"要不要 await"**
4. **`asyncio.run(main())` 缩进进函数体**:脚本只定义函数从不调用 → 零输出
5. **httpx 本地测试 502**:httpx 默认 `trust_env=True` 读系统代理 → 127.0.0.1 也被丢进代理 → 代理返回 502(uvicorn 从不返回 502,502 是代理/网关错误码)。修:`httpx.AsyncClient(trust_env=False)`
6. **`--reload` 默认监视整个目录**:保存测试脚本/IDE 触碰文件都会触发重启 → 撞上重启窗口 502。修:启动加 `--reload-dir app`
7. **Bearer 头少空格**:`f"Bearer{token}"` → 应为 `f"Bearer {token}"`;没空格 FastAPI 解析不出 token → 一律 401

### 验证
`test_conversations.py` 15 项全绿:注册 201×2、创建会话 201、发消息 201(role=user)、消息列表 200(1→2 条)、会话列表 200、详情 200、**越权三连 404**(B 用 A 的会话 id 访问/发消息/看消息)、删除 204、删后再查 404。

---

## 第七课:文档上传 → 解析 → 切片(2026-08-02)

### 成果
- `app/models/document.py` — 文档表(1 用户 → N 文档,外键级联删除)
- `app/services/parser.py` — `parse_document`:PDF(pypdf)/txt/md → 纯文本
- `app/services/chunker.py` — `chunk_text`:滑动窗口切片(size=500, overlap=50)
- `app/schemas/document.py` — DocumentOut(响应不含 text_content)
- `app/api/routers/documents.py` — `POST /documents/upload`(三道校验 + UUID 落盘 + 解析入库)
- `test_documents.py` — 上传简历 PDF 全链路验证

#### 上传接口核心(安全版)
```python
content = await file.read()
# 三道校验全前置:大小(413) → 类型白名单(400) → 数量 COUNT(409)
clean_filename = Path(file.filename or "").name      # 净化:剥掉目录,防路径穿越
stored_name = f"{uuid4()}{ext}"                      # UUID 重命名,防覆盖/猜路径
storage_path.parent.mkdir(parents=True, exist_ok=True)  # write_bytes 不建父目录!
storage_path.write_bytes(content)
doc = Document(..., text_content=parse_document(storage_path))
```

### 知识点
1. **上传三件套**:`UploadFile`(异步,必须 `await file.read()`)、`python-multipart`、httpx 的 `files=` 参数
2. **三道校验**:类型白名单(400)/ 大小 ≤10MB,413 Payload Too Large / 数量 ≤50,409(COUNT 查询)。**校验全前置,失败零痕迹**
3. **路径穿越防护(面试重点)**:`file.filename` 不可信(可伪造 `../../../etc/passwd`)→ `Path().name` 净化 + `uuid4()` 重命名存储 + 按用户分目录。实测:multipart 标签带目录"简历/简历.pdf" → 入库仅"简历.pdf"
4. **`write_bytes` 不建父目录** → 必须先 `storage_path.parent.mkdir(parents=True, exist_ok=True)`(实测 FileNotFoundError)
5. **LONGTEXT vs TEXT**:MySQL TEXT 上限 64KB,长文本用 LONGTEXT(4GB)
6. **pypdf 三步**:`PdfReader(路径)` → `reader.pages` → `page.extract_text() or ""`(可能返回 None)
7. **滑动窗口切片**:size=500/overlap=50;overlap 保相邻块上下文;尾块 `min()` 截断 + `end==len` 退出
8. **防御性编程**:参数校验(overlap<size 防死循环),用 `raise ValueError` 而非 `assert`(-O 会禁用)
9. **pathlib 全家桶**:`read_text`/`write_bytes` 是 `open()` 的语法糖;`Path(__file__).resolve().parents[3]` 定位项目根(不依赖工作目录)
10. **数量校验**:`select(func.count()).select_from(Document).where(...)` 查行数;自增 id ≠ 数量

### 踩过的坑
1. **扩展名判断**:`Path.suffix` 返回带点(`".pdf"`),直接比 `"pdf"` 永远 False → PDF 分支不执行
2. **尾块丢失 + 死循环(边界条件必手推)**:`while end<=len` 丢尾块;改成 `while start<len` 后,`end` 触底时 `start=end-overlap` 回退 → 死循环。修:`if end == len(text): break`。排查法:Ctrl+C 看卡在哪行 + 手推最后几轮迭代
3. **`write_bytes` 不建父目录** → 首次上传 FileNotFoundError
4. **`doc.id > MAX_DOCS_PER_USER`**:自增 id 不是"用户文档数";数量要 COUNT 查询且放在写文件/入库之前
5. **httpx 测试缺 `trust_env=False`**(第二次踩) → 代理 502
6. **测试脚本缺 print**:静默成功无法区分"跑了"和"没跑"
7. **f-string 套 Path、Path 套 Path**:能跑但绕

### 验证
- 上传简历.pdf(131777 字节):DB 记录 `filename='简历.pdf'`(路径穿越净化生效)、`file_size=131777`、磁盘 `uploads/6/{uuid}.pdf` 字节一致、`text_content` 1322 字解析正确
- 防御实测:multipart 标签带目录 "简历/简历.pdf" → 入库仅 "简历.pdf"

---

## 第八课:Embedding + Qdrant 向量检索 + RAG 问答(2026-08-03)

### 成果
- 环境:sentence-transformers + BGE(`bge-small-zh-v1.5`,512 维)、qdrant-client 1.18、openai SDK(OpenAI 兼容协议调 DeepSeek)
- `app/services/vector_store.py` — Qdrant 封装四函数:
  - `ensure_collection` — 幂等建库(512 维 cosine)+ user_id/document_id payload 索引
  - `upsert_chunks` — 批量编码 + payload 五件套 + **先删后插**幂等
  - `search_chunks` — `query_points` + user_id 过滤(隔离)
  - `delete_document_vectors` — filter 双条件删除
- `app/services/rag.py` — `generate_answer`:检索 → 编号拼上下文 → DeepSeek → `{answer, sources}`
- 配置:QDRANT_URL / QDRANT_COLLECTION / EMBEDDING_DIM;`.env` 的 EMBEDDING_MODEL 改为 BGE

#### collection 设计(自己设计的方案)
```
ai_knowledge_chunks:512 维,cosine
payload:user_id / document_id / chunk_index / content / filename
索引:user_id、document_id(INTEGER);point id = uuid4()
删除联动:filter(user_id + document_id) 双条件
```

#### RAG 核心(rag.py)
```python
llm = OpenAI(api_key=get_settings().DEEPSEEK_API_KEY, base_url=get_settings().DEEPSEEK_BASE_URL)  # 模块级单例

def generate_answer(client, user_id, question, top_k):
  hits = search_chunks(client, user_id, embedder().encode(question).tolist(), top_k)
  for index, hit in enumerate(hits, start=1):
    context += f"[{index}] {filename} chunk {chunk_index}\n{content}\n\n"
    sources.append({"filename": ..., "chunk_index": ...})
  messages = [{"role": "system", "content": 规则}, {"role": "user", "content": "资料:\n"+context+"\n\n问题:"+question}]
  reply = llm.chat.completions.create(model=..., messages=messages)
  return {"answer": reply.choices[0].message.content, "sources": sources}
```

### 知识点
1. **Embedding**:文本→向量;方向编码语义,模长受长度/词频影响。**余弦 vs 欧氏**:余弦只看方向;归一化后等价(`d²=2-2cosθ`,亲手数据验证);BGE 输出已归一化(Normalize 模块焊在模型管线——"把正确用法做成默认"的防呆设计)
2. **Qdrant 概念**:collection≈表 / point≈行 / vector≈向量列 / payload≈普通列;**payload 索引**≈MySQL 索引,filter 高频路径必须建(多租户 user_id 过滤是命门)
3. **多租户隔离**:共享 collection + user_id filter(主流);不要按用户分 collection(固定开销+运维噩梦)。隔离有效性用数据验证:user2 搜索 → `[]`
4. **qdrant-client 1.18 API**:没有 `search`,用 `query_points(query=向量, query_filter=Filter, limit=)`;结果在 `.points` 里。**API 以 `dir()` + `inspect.signature` 为准,网上老教程会骗人**
5. **懒加载单例**:无参函数 + `@lru_cache`(模型只加载一次,与 `get_settings` 同套路);缓存"模型对象",不是"编码结果"
6. **openai 兼容协议**:`OpenAI(api_key, base_url)` 调 DeepSeek;messages 三角色(system/user/assistant);**system 放规则**(优先级高 + 用户无法覆盖,防 prompt 注入)
7. **RAG 组装与溯源**:`[n]` 编号 ↔ sources 一一对应;sources 由**代码记录检索结果**(不让模型标引用——模型会编造);溯源链:编号 → sources → 前端展示
8. **分层依赖**:rag(业务层)→ vector_store(基础层)单向依赖;循环导入 = 反向依赖的信号
9. **pydantic-settings 优先级**:环境变量 > `.env` > 类默认值;`env_file=".env"` 相对**当前工作目录**
10. **编辑器红线 vs 运行时**:`dict[str,str]` vs `ChatCompletionMessageParam`(TypedDict)——运行时无碍,标注官方类型消红线;判断原则:运行时会炸吗?

### 踩过的坑
1. **conda 装 pytorch `InvalidArchiveError`**:包其实完好(zip + zstd 双重验证通过),是 conda 解压器兼容性问题 → 改 pip 装(ML 生态官方分发渠道是 pip wheel)
2. **"下载截断"假象**:两次下载同字节 → 用 Content-Length 核对,34MB 就是包真实大小;以服务器声明为准,别凭记忆猜
3. **pydantic 字段必须注解**:`QDRANT_URL = "..."` 没写 `: str` → PydanticUserError
4. **qdrant-client 502(代理)**:httpx 走代理劫持 localhost 请求(空响应体 502);时好时坏(代理开关);A/B 测试 `trust_env=True/False` 定位;根治:no_proxy 或 trust_env=False
5. **HF 连接超时**(WinError 10060):`HF_ENDPOINT=https://hf-mirror.com` 或 `HF_HUB_OFFLINE=1`(模型已缓存时秒开)
6. **配置只做一半**:代码读 settings,但 `.env` 的 EMBEDDING_MODEL 仍是 text-embedding-3-small → 报错信息里能看到模型名,顺藤摸瓜
7. **循环导入**:vector_store ↔ rag 互导 → `cannot import name ... partially initialized`
8. **返回值没接住 ×3**:`OpenAI(...)` 没赋值、`create()` 没接、`encode()` 结果扔掉——**每次调用先问"返回值接住了吗"**
9. **if 条件写反**:`if hit is not None: continue` → 上下文永远为空
10. **answer 返回整个 reply 对象**:要 `choices[0].message.content`
11. **脚本方式 vs 模块方式**:`python rag.py` 找不到 `app` 包;必须 `python -m app.services.rag` 且站在项目根目录
12. **numpy 首次导入慢**(冷启动+杀软扫描):别急着 Ctrl+C
13. ⚠️ **密钥进代码(最危险)**:DeepSeek key 一度写进 config.py 默认值 → config.py 进 git,公开仓库会泄露;key 只能放 .env(已 gitignore)

### 验证
- 冒烟:插入 3 → 重插 3(幂等);user1 搜索 top1 = 自己(0.922,提问版 query);**user2 搜索 `[]`(隔离生效)**
- RAG:"FastAPI 基于什么实现异步?" → answer「FastAPI 基于 asyncio 实现异步 [1]」+ sources 3 条(含 test.txt chunk 0)
- 编码确定性实测:单条 vs 批量 encode 最大差 1e-7(浮点噪声)

---

## 第九课:接口接入(上传即入库 / 会话内 RAG 问答 / 引用溯源入库)(2026-08-03)

### 成果
- `documents.py`:
  - `POST /documents/upload` — **上传即入库**:parser → chunker → upsert_chunks 串联 + 失败补偿
  - `GET /documents` — 文档列表(归属过滤 + 倒序)
  - `DELETE /documents/{id}` — **删除联动**(删 Qdrant 向量 + 删 MySQL,204)
- `conversations.py` `add_message` — **发消息即问答**:查历史(最近 10 条)→ 存 user → generate_answer → 存 assistant(sources 入库)
- `models/message.py` — messages 表加 `sources` JSON 列(引用溯源落库,user 消息为 NULL)
- `schemas/conversation.py` — MessageOut 加 sources 字段
- `rag.py` — generate_answer 加 `history` 参数(prompt 三块:资料 → 历史 → 问题)
- `test_rag_api.py` — 全链路测试

### 知识点
1. **async 阻塞问题(本课灵魂)**:FastAPI 路由是 async,同步慢操作(encode/LLM/Qdrant)会阻塞事件循环 → `await asyncio.to_thread(函数, 参数...)` 丢线程池。**async 解决 I/O 等待,不解决 CPU 密集**——BGE encode 没有异步版,两条路都绕不开线程池;to_thread 第一参数是函数本身(不带括号),只包真正慢的调用
2. **跨系统无事务**:MySQL 和 Qdrant 是两个系统,不能原子提交 → "先做一边,失败补偿另一边"(上传:先 MySQL 拿 id → 入库向量 → 失败则删 doc + 删向量);**补偿代码也可能失败**(collection 不存在时删向量再抛)→ 补偿要考虑边缘
3. **会话历史(多轮上下文)**:messages 表查最近 N 条(`order_by(id.desc()).limit(10)` → 反转成正序)→ 拼进 prompt 的资料后面。**历史负责理解语境("它"指什么),资料负责事实依据;冲突时以资料为准**
4. **`db.scalar` vs `db.scalars`**:scalar = 单个值(查数量/单个对象,可能 None,参与比较用 `or 0`);scalars = 结果集(要 `.all()`,带 s = 复数 = 一堆)
5. **响应验证错误**:`input: None` = 函数没 return → response_model 无法序列化 → 500(排查:先想到"是不是没返回")
6. **删除联动三处存储**:MySQL + Qdrant + 磁盘文件——真实产品都要删(MVP 磁盘暂未删);**幂等让顺序不敏感**(delete_document_vectors 幂等,先删哪边都行)
7. **httpx 响应分层**:`r.content` 是原始字节、`r.json()` 才是业务数据、`r.history` 是重定向历史不是对话历史
8. **字符串拼接用 join 不用 +=**(字符串不可变,+= 反复新建)
9. **改模型 ≠ 自动生效**:create_all 只建不存在的表,已有表要手动 ALTER 或 drop 重建;`Mapped[非 Optional]` → NOT NULL(user 消息没有 sources,必须 nullable)

### 踩过的坑
1. **upload 缺 `return doc`**(重构 try/except 时弄丢)→ 500 `input: None`——"重构顺手删东西"的 bug,报错位置(序列化)离病灶(没返回)十万八千里,排查看日志的 input 值
2. **路由前缀重复**:router 已有 `prefix="/documents"`,再写 `@router.get("/documents")` → 实际路径 `/documents/documents`
3. **删除查询漏条件**:只 `where(user_id)` → 删的是"用户第一份文档";必须 `id + user_id` 双条件
4. **schema 字段拼错**:`soureces` → `sources`(typo 导致 from_attributes 匹配不上,响应永远 None)
5. **get_settings 从错误模块导入**(应 `app.core.config`,别从 vector_store 顺带拿)
6. **孤儿数据假象**:ReadError 崩溃运行遗留的文档/向量,误判为删除联动失败——先查数据归属(users 最新 id)再下结论
7. **BGE 首次加载卡死**:uvicorn 里模型首次加载 + HF 网络检查(重试 5×20s)→ 起服务前 `$env:HF_HUB_OFFLINE = "1"`

### 验证
- 全链路:注册 201 → 上传 200 → 建会话 201 → 问答 201×2 → 列表 200 → 删除 204
- **历史生效**:第二条问「它支持异步吗?」→「**是的**,FastAPI 基于 asyncio 实现异步...」——"是的"证明模型接住了"它"=FastAPI
- **引用溯源落库**:assistant 消息 sources 列 = `[{"filename": "test.txt", "chunk_index": 0}]`,user 消息为 NULL
- **删除联动**:删后 MySQL 文档消失 + Qdrant 对应向量消失(双查证实)

---

## Git 提交记录

| 提交 | 说明 |
|---|---|
| `db0dfd3` | feat: FastAPI 最小应用,健康检查接口 |
| `d6440b1` | feat: 项目分层结构 + pydantic-settings 配置管理 |
| `99a227a` | feat: SQLAlchemy 异步数据库连接(引擎+会话+依赖) |
| `1b52c3b` | feat: ORM 基类 + User 模型,建表到 MySQL;添加项目历程文档 |
| `191c644` | chore: 添加 .gitignore 和建表脚本 |
| `83f1c02` | feat: bcrypt 密码哈希 + JWT 签发与验证 |
| `928aec6` | feat: bcrypt 密码哈希与验证 |
| `5401bc7` | feat: 添加 JWT 签名算法配置 |
| `cae5cbc` | feat: 注册登录接口(bcrypt 密码哈希 + JWT 认证) |
| `a4fccda` | docs: 第五课认证接口学习回顾 |
| `db4d7d5` | feat: 第六课 会话与消息模型 + 当前用户接口 + 会话CRUD |
| `f051fe9` | feat: 第七课 文档上传→解析→切片(路径安全+三道校验+滑动窗口) |
| `472ad69` | feat: 第八课 Qdrant 向量库(建库/入库/检索/隔离)+ BGE 嵌入配置 |
| `8d4f4df` | feat: 第八课 RAG 问答链路(检索→拼装→DeepSeek→引用溯源) |
| `24ea0c0` | feat: 第九课 接口接入(上传即入库/会话内RAG问答/引用溯源/删除联动) |
| `bdb8a42` | feat: 第十课 Vue 3 前端脚手架 + Streamlit 版登录页(对照保留) |
| `583dc88` | docs: 第十课 Vue 版进度 + 提交号回填 |
| `ccb82e3` | feat: 第十课 CORS 配置 + Vue 前端登录/上传页开发 |
| `7f771d8` | feat: 第十课 Vue 前端-聊天功能+会话/文档删除+ChatGPT布局 |

---

## 常用命令速查

```powershell
# 启动服务(先设环境,避免 qdrant 走代理 502 + 模型加载卡 HF)
$env:no_proxy = "localhost,127.0.0.1"
$env:HF_HUB_OFFLINE = "1"
python -m uvicorn app.main:app --reload --reload-dir app   # --reload-dir 只监视 app 目录,测试脚本/IDE 不会触发重启

# 建表
python -m app.init_db

# 查 MySQL
docker exec ai-knowledge-mysql mysql -uapp -papp123456 ai_knowledge -e "SHOW TABLES;"
docker exec ai-knowledge-mysql mysql -uapp -papp123456 ai_knowledge -e "DESC users;"

# Qdrant(容器 ai-knowledge-qdrant,端口 6333)
curl http://localhost:6333/collections/ai_knowledge_chunks   # 查看 collection 状态

# 看端口占用
Get-NetTCPConnection -LocalPort 8000 -State Listen

# git
git add <文件> && git commit -m "..." && git push
```

---

## 下一步计划

- [x] 第五课:认证接口(注册/登录)— 密码哈希(bcrypt)+ JWT
- [x] 第六课:会话与消息模型 + 获取当前用户接口(依赖注入实战)
- [x] 第七课:文档上传 → 解析 → 切片(原料生产线)
- [x] 第八课:Embedding + Qdrant 向量检索 + RAG 问答(向量库 + RAG 链路)
- [x] 第九课:接口接入(上传即入库、会话内 RAG 问答、引用溯源落库、删除联动)
- [x] 第十课:Vue 3 前端(脚手架/组件化/CORS 配置/登录/上传/聊天/引用展示全部完成)
- [ ] 第十一课:SSE 流式输出(打字机效果,当前是等整段返回)
- [ ] 第十二课:管理员系统(RBAC 角色 + 用户管理,可删除普通用户)
- [ ] 第十三课:检索质量优化(Rerank + BM25 混合检索)
- [ ] 自动化测试补全(pytest)+ Docker 部署(前后端 + MySQL + Qdrant 一键起)

---

## 后续功能规划(2026-08-05)

> 方向:体验(流式) → 权限(管理员) → 质量(检索) → 性能(高并发) → 工程(测试/部署) → 多模态(远期)

### 一、第十一课:SSE 流式输出(打字机效果)

**现状**:聊天接口等 DeepSeek 整段返回后才响应,体验一般。

**方案**:
- 后端:FastAPI `StreamingResponse`(media_type=`text/event-stream`),把 DeepSeek 的流式响应(`stream=True`)逐 chunk 转发;消息落库放在流结束后(保证失败不丢)
- 前端:fetch 的 `ReadableStream` 逐段渲染;注意 SSE 规范下 EventSource 不能带 Authorization 头 → 用 POST + fetch stream 或 query 传 token
- 坑预期:连接中断时消息完整性(落库兜底);打字机光标显隐;多轮上下文仍用完整历史

**面试点**:SSE vs WebSocket 取舍(单向推送够用,不用双工)

### 二、第十二课:管理员系统(新增需求:管理员可删除普通用户)

**需求**:需要一个"数据库管理员"角色,可查看/删除普通用户。

**设计**(RBAC 权限模型):
1. **角色字段**:`users` 表加 `role`(`user`/`admin`,默认 `user`);注册接口固定为 `user`,只能由管理员提升
2. **种子管理员**:`init_db` 时从 `.env` 读 `ADMIN_USERNAME/ADMIN_PASSWORD` 创建(幂等,已存在则跳过)
3. **权限依赖**:`require_admin` 依赖 = 复用 `get_current_user` + 校验 `role=='admin'`,非管理员统一 403
4. **管理接口**:
   - `GET /api/v1/admin/users` 用户列表(分页,展示注册时间/文档数)
   - `DELETE /api/v1/admin/users/{id}` 删除用户(联动清理)
5. **删除用户联动清理**(重点,跨存储一致性):
   - MySQL:删 user → 其 conversations → messages → documents 记录
   - 文件系统:删 `uploads/{user_id}/` 目录
   - 向量库:Qdrant 按 `user_id` payload filter 批量删该用户全部向量
   - 顺序权衡:向量库无事务 → 先删向量/文件,再删 DB;失败可重试
6. **防呆**:管理员不能删除自己;至少保留一个 admin;前端二次确认弹窗

**面试点**:RBAC 角色模型、跨存储级联删除一致性(MySQL+文件+向量库)、依赖注入做权限控制

### 三、第十三课:检索质量优化(Rerank + 混合检索)

**现状**:单路向量检索(top-k=5),长文档/全局性问题召回不足。

**方案**:
- 混合检索:BM25(关键词)+ 向量(语义)双路召回,RAGFusion 式加权合并
- Rerank:召回 20~50 条后本地 `bge-reranker-v2-m3` 精排取 top-k
- 评估:建小问答测试集,对比"纯向量 vs 混合+重排"的命中率/回答质量

**面试点**:召回-精排两阶段范式(与推荐系统同构)、混合检索互补性(关键词精确 vs 语义泛化)

### 四、高并发与性能优化(工程向)

- **连接池**:SQLAlchemy `pool_size`/`max_overflow` 调优;Qdrant 客户端连接复用
- **缓存**:热点问答/检索结果缓存(内存 LRU 或 Redis),命中直接返回
- **限流**:按用户/IP 限流(`slowapi`),防刷接口
- **分页**:文档/会话列表加分页(现在全量返回,数据多会慢)
- **SSE**:首字延迟摊薄(见第十一课)

**面试点**:异步事件循环+协程的并发模型、连接池参数对吞吐的影响

### 五、工程质量

- **自动化测试**:把现有 `test_*.py`(httpx 全流程)整理成 pytest 用例:注册/登录/上传/问答/越权/删除联动/多租户隔离
- **Docker 部署**:后端 Dockerfile + 前端(nginx 托管构建产物)+ `docker-compose.yml` 一键起 mysql+qdrant+backend+frontend;`.env` 用环境变量注入
- **CI(可选)**:GitHub Actions 跑 pytest

### 六、多模态 RAG(远期,调研中)

- 参考 `D:\work\论文`:ColPali(页面图直接嵌入检索)/ M3DocRAG(多模态端到端引用)/ GraphRAG(图谱检索)
- 目标:支持带图表/表格的 PDF 直接按页检索,扩展引用溯源到图片证据
