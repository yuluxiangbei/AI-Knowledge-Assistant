# 项目历程(学习回顾)

> 记录从零开发 AI-Knowledge-Assistant 的每一步:知识点、踩过的坑、验证方式。
> 更新频率:每完成一课更新一次。

---

## 环境准备

- **Python 环境**:conda 环境 `AI-Knowledge`(Python 3.13.9),路径 `D:\anaconda3\envs\AI-Knowledge\python.exe`
- **数据库**:MySQL 8.4 跑在 Docker 容器 `ai-knowledge-mysql`(端口 3306,库 `ai_knowledge`,账号 `app/app123456`,root 密码 `root123456`)
- **远程仓库**:GitHub `yuluxiangbei/AI-Knowledge-Assistant`(SSH key:`~/.ssh/github_ed25519`)
- **重要**:conda 的 `activate` 在 PowerShell 里需要先 `conda init powershell` 才能生效;不生效时用全路径 `D:\anaconda3\envs\AI-Knowledge\python.exe` 直接调用

---

## 第一课:FastAPI 最小应用(2026-08-01)

### 成果
`app/main.py` — FastAPI 实例 + `GET /health` 返回 `{"status":"ok"}`

```python
from fastapi import FastAPI

app = FastAPI()

@app.get("/health")
def get_health():
  return {"status":"ok"}
```

### 知识点
1. FastAPI 实例:`app = FastAPI()`
2. 路由装饰器:`@app.get("/health")`
3. 返回字典自动转 JSON
4. 启动命令:`python -m uvicorn app.main:app --reload`
   - `app.main` = app 包里的 main 模块
   - `app`(冒号后)= 模块里的应用实例名
5. **文件扩展名必须是 `.py`**,否则 Python 不认它是模块

### 踩过的坑
- 文件名写成 `main` 没有 `.py` 扩展名 → uvicorn 无法启动
- 端口被占报 `WinError 10013/10048` → 检查 `Get-NetTCPConnection -LocalPort 8000` 找到占用进程杀掉

### 验证
浏览器访问 `http://127.0.0.1:8000/health` → `{"status":"ok"}`

---

## 第二课:项目分层 + 配置管理(2026-08-01)

### 成果
- 目录结构:`app/core`(配置/安全)、`app/db`(数据库)、`app/api/routers`(接口)、`app/schemas`(校验)、`app/models`(表模型)、`app/services`(业务)
- 每个目录放 `__init__.py` 让 Python 识别为包
- `app/core/config.py` — pydantic-settings 配置管理

```python
from functools import lru_cache

from pydantic_settings import BaseSettings, SettingsConfigDict


class Settings(BaseSettings):
    model_config = SettingsConfigDict(env_file=".env", env_file_encoding="utf-8")

    APP_NAME: str = "AI-Knowledge-Assistant"
    SECRET_KEY: *** = "change-me"
    DATABASE_URL: str = "mysql+asyncmy://app:app123456@127.0.0.1:3306/ai_knowledge?charset=utf8mb4"
    # ... 其他配置项


@lru_cache
def get_settings() -> Settings:
    return Settings()
```

### 知识点
1. **config.py 解决什么问题**:把"会变的东西"(密钥、数据库地址)从代码里抽出来。代码只写默认值,真实值放 `.env`。`.env` 有值 → 覆盖默认值;没有 → 用默认值
2. 好处:换环境只改 `.env` 不改代码;密钥不进 git 不泄露;配置集中管理
3. `SettingsConfigDict(env_file=".env")`:告诉 pydantic 去 .env 读取
4. `@lru_cache`:配置对象只创建一次,以后调用走缓存
5. 类型注解很重要:`int` 类型会自动把 `.env` 的字符串转成整数

### 踩过的坑
- `.env` 里有的变量,`Settings` 类里没定义 → 报 `Extra inputs are not permitted`。解决:把配置项全部声明到类里(规范做法),或在 `SettingsConfigDict` 加 `extra="ignore"`(偷懒做法)
- 导入顺序规范:标准库(`functools`)在最上,第三方库在下面(风格问题,不影响运行)

### 验证
```powershell
python -c "from app.core.config import get_settings; s = get_settings(); print(s.APP_NAME)"
```

---

## 第三课:数据库连接(2026-08-01)

### 成果
`app/db/session.py` — 异步引擎 + 会话工厂 + FastAPI 依赖

```python
from collections.abc import AsyncGenerator

from sqlalchemy.ext.asyncio import AsyncSession, async_sessionmaker, create_async_engine

from app.core.config import get_settings

settings = get_settings()

engine = create_async_engine(
  settings.DATABASE_URL,   # 从配置读连接串
  echo = False,            # True 会打印所有 SQL(调试用)
  pool_pre_ping = True,    # 借连接前先 ping,防止用上死连接
)

AsyncSessionLocal = async_sessionmaker(engine, expire_on_commit=False)

async def get_db() -> AsyncGenerator[AsyncSession, None]:
  async with AsyncSessionLocal() as session:
    yield session
```

### 知识点(本课最核心)
1. **三层结构**:
   - `engine` = 连接池管家,应用启动建一次,全局共用,绝不每个请求重建
   - `AsyncSessionLocal` = 会话工厂,每次要"对话"时拿一个会话
   - `get_db()` = FastAPI 依赖,请求来 → 开会话 → 给路由用 → 结束自动关
2. **`async with`**:进入时自动初始化,退出时自动关闭(即使中途报错也会关)。防连接泄漏的保险
3. **`yield` vs `return`**:
   - `return` = 交完就死,函数结束,收尾代码永远不执行 → 连接泄漏
   - `yield` = 交完待命,函数暂停但活着,路由用完唤醒继续收尾
   - get_db 用 yield 是因为**要保证事后关闭会话**
4. **`expire_on_commit=False`**:commit 后对象属性不过期,不用重新查库
5. **`pool_pre_ping=True`**:MySQL 连接闲置久了会断,借出前先试探,防"死连接"灵异 bug
6. **async + yield 组合**:`async def` 管 await(异步操作),`yield` 管暂停交出。只有 async 没 yield 跑完就结束没人收尾;只有 yield 没 async 没法 await 异步关闭。两者都要 → 异步生成器

### 踩过的坑
- `async_sessionmaker` 参数名拼错:`engine_on_commit` → 正确是 `expire_on_commit`(拼写错误用 IDE 提示避免)
- PowerShell 命令行写多行 Python 引号冲突 → 改用脚本文件 `test_db.py`

### 验证
```python
# test_db.py
import asyncio
from sqlalchemy import text
from app.db.session import engine

async def main():
    async with engine.connect() as conn:
        result = await conn.execute(text("SELECT 1"))
        print("数据库连接成功:", result.scalar())
    await engine.dispose()

asyncio.run(main())
```

---

## 第四课:ORM 模型 + 建表(2026-08-01)

### 成果
- `app/db/base.py` — 所有模型的基类

```python
from sqlalchemy.orm import DeclarativeBase

class Base(DeclarativeBase):
  pass
```

- `app/models/user.py` — User 模型

```python
from sqlalchemy import String
from sqlalchemy.orm import Mapped, mapped_column

from app.db.base import Base

class User(Base):
  __tablename__ = "users"

  id: Mapped[int] = mapped_column(primary_key=True, autoincrement=True)
  username: Mapped[str] = mapped_column(String(64), unique=True, nullable=False)
  hashed_password: Mapped[str] = mapped_column(String(255), nullable=False)
```

- `app/models/__init__.py` — 汇总导出

```python
from app.models.user import User
```

- `app/init_db.py` — 建表脚本

```python
import asyncio

from app import models  # noqa: F401  确保模型被导入注册
from app.db.base import Base
from app.db.session import engine


async def init():
    async with engine.begin() as conn:
        await conn.run_sync(Base.metadata.create_all)


if __name__ == "__main__":
    asyncio.run(init())
```

### 知识点
1. **ORM = 对象关系映射**:用 Python 类描述表,SQLAlchemy 翻译成 SQL。好处:不手写 SQL、换库不改代码、查询结果是对象、防 SQL 注入
2. `Mapped[int]` / `Mapped[str]`:类型注解 → 列类型(int→INTEGER, str→VARCHAR)
3. `String(64)`:限定长度(对应 VARCHAR(64))。`hashed_password` 用 255 因为 bcrypt 哈希 60 字符,留余量
4. `unique=True` → 数据库唯一索引(UNI),并发下也不会插入重复数据
5. **`__init__.py` 汇总导出**:模型必须被导入过,SQLAlchemy 才知道它的存在。`from app import models` 只加载 `__init__.py`,不写导入行 → User 不会被注册 → `create_all` 无事可做
6. **模型与连接分离**:models 只依赖 Base 不碰 engine,避免循环依赖,可复用

### 踩过的坑
1. `python app/init_db.py` 报 `ModuleNotFoundError: No module named 'app'`:
   - `python file.py` = 把文件所在目录当工作目录 → 找不到 app 包
   - `python -m app.init_db` = 从当前目录找 → 找得到
   - 项目脚本统一用 `-m` 方式(uvicorn 也是)
2. 数据库环境残留:推倒重来后,MySQL 里还有旧表。`create_all` 发现表存在就跳过,不会重建
   - 清理:`docker exec ai-knowledge-mysql mysql -uroot -proot123456 -e "DROP DATABASE ai_knowledge; CREATE DATABASE ai_knowledge;"`
   - 教训:**代码推倒重来 ≠ 数据库自动清空**
3. 建表后 `SHOW TABLES` 为空 → 排查出是 `__init__.py` 没导入模型

### 验证
```powershell
python -m app.init_db
docker exec ai-knowledge-mysql mysql -uapp -papp123456 ai_knowledge -e "SHOW TABLES;"
docker exec ai-knowledge-mysql mysql -uapp -papp123456 ai_knowledge -e "DESC users;"
```

最终表结构:
| Field | Type | Null | Key | Default | Extra |
|---|---|---|---|---|---|
| id | int | NO | PRI | NULL | auto_increment |
| username | varchar(64) | NO | UNI | NULL | |
| hashed_password | varchar(255) | NO | | NULL | |

---

## 第五课:认证接口(注册/登录)(2026-08-01)

### 成果
- `app/core/security.py` — bcrypt 密码哈希 + JWT 签发/验证
- `app/schemas/auth.py` — 请求/响应模型
- `app/api/deps.py` — DbDep 数据库依赖
- `app/api/routers/auth.py` — 注册/登录路由
- `app/api/routers/__init__.py` + `app/main.py` — 路由挂载

#### security.py(密码哈希)

```python
import bcrypt


def hash_password(plain: str) -> str:
  passwd = bcrypt.hashpw(plain.encode("utf-8"), bcrypt.gensalt())
  return passwd.decode()


def verify_password(plain: str, hashed: str) -> bool:
  result = bcrypt.checkpw(plain.encode("utf-8"), hashed.encode("utf-8"))
  return result
```

#### security.py(JWT)

```python
from datetime import datetime, timedelta, timezone
import jwt
from app.core.config import get_settings

settings = get_settings()


def create_access_token(subject: str | int) -> str:
  exp = datetime.now(timezone.utc) + timedelta(minutes=settings.ACCESS_TOKEN_EXPIRE_MINUTES)
  payload = {"sub": str(subject), "exp": exp}
  return jwt.encode(payload, settings.SECRET_KEY, algorithm=settings.ALGORITHM)


def decode_access_token(token: str) -> dict:
  return jwt.decode(token, settings.SECRET_KEY, algorithms=[settings.ALGORITHM])
```

#### auth.py 路由(核心逻辑)

```python
@router.post("/register", response_model=TokenOut, status_code=status.HTTP_201_CREATED)
async def register(payload: RegisterRequest, db: DbDep):
  existing = await db.scalar(select(User).where(User.username == payload.username))
  if existing:
    raise HTTPException(status_code=409, detail="用户名已存在")
  user = User(username=payload.username, hashed_password=hash_password(payload.password))
  db.add(user)
  await db.commit()
  await db.refresh(user)
  return TokenOut(access_token=create_access_token(user.id), user=UserOut.model_validate(user))


@router.post("/login", response_model=TokenOut)
async def login(payload: LoginRequest, db: DbDep):
  user = await db.scalar(select(User).where(User.username == payload.username))
  if not user or not verify_password(payload.password, user.hashed_password):
    raise HTTPException(status_code=401, detail="用户名或密码错误")
  return TokenOut(access_token=create_access_token(user.id), user=UserOut.model_validate(user))
```

### 知识点
1. **为什么密码不能明文存库**:数据库被拖库则全部泄露;很多人一个密码用所有网站,连累其他账号。解决方案:哈希(不可逆)+ 加盐(相同密码不同哈希,防彩虹表)。bcrypt = 加盐 + 故意算得慢(暴力破解成本高)
2. **bcrypt 哈希串结构**:`$2b$12$盐(22字符)哈希值` — 版本 + 计算成本(2^12)+ 盐 + 哈希
3. **盐藏在哈希里**:gensalt 随机 → 每次哈希结果不同;checkpw 从哈希中取出盐重算比对 → 验证总能成功
4. **JWT 三段结构**:头部(算法).载荷(数据).签名(防篡改)。签名 = 内容 + SECRET_KEY 的"印章"
5. **JWT 流程**:登录成功 → 发证;后续请求带证;服务器验签不查库(签名自证)
6. **sub 存字符串**(JWT 规范要求)、**exp 用 UTC**(服务器可能在任意时区)
7. **防篡改实测**:改 token 一个字符 → `InvalidSignatureError`
8. **密钥安全**:SECRET_KEY ≥32 字节、随机、藏 .env 不进 git(实测 9 字节触发 InsecureKeyLengthWarning)
9. **Pydantic 校验**:`Field(min_length=...)` 请求格式;`ConfigDict(from_attributes=True)` 支持 ORM→响应模型转换
10. **token_type="bearer"**:OAuth2 约定,告诉客户端令牌以 `Authorization: Bearer <token>` 携带
11. **安全细节**:UserOut 不暴露 hashed_password;用户不存在/密码错误统一 401(防用户名枚举);重复注册 409
12. **from_attributes 缺失后果**:返回 ORM 对象时 FastAPI 不知道去哪取值,直接报错

### 踩过的坑
1. **类名拼写错误**:`LoginReqquest` 多一个 q → 导入报 ImportError。避免:写完跑 `python -c "from app.schemas.auth import *"` 验证 + IDE 波浪线提示
2. **参数名与模型字段不一致**:`User(hash_password=...)` 应为 `User(hashed_password=...)` → TypeError
3. **登录逻辑反了**:`verify_password(明文, 新生成的哈希)` 恒为 True → 永远 401。正确:`not verify_password(明文, user.hashed_password)`
4. **tags 拼错** `"ayth"` → Swagger 分组名错
5. **python -c 长命令在 PowerShell 折行** → 改脚本文件(test_auth.py)
6. **运行目录不对** → `No module named 'app'`。所有 `python -m`/`from app...` 都在项目根目录跑
7. **502 + JSONDecodeError**:服务器未启动或 --reload 重载中请求 → 空响应。先确认 `8000 端口在监听` 再测
8. **mysql -e 命令被 PowerShell 折行** → `-e` 后的 SQL 要写在同一行

### 验证
```python
# test_auth.py(临时)
import asyncio
import httpx

async def main():
    async with httpx.AsyncClient() as client:
        r1 = await client.post("http://127.0.0.1:8000/api/v1/auth/register",
                               json={"username": "alice3", "password": "secret123"})
        print("注册状态码:", r1.status_code)      # 201
        print("注册响应:", r1.json())
        r2 = await client.post("http://127.0.0.1:8000/api/v1/auth/register",
                               json={"username": "alice3", "password": "secret123"})
        print("重复注册状态码:", r2.status_code)   # 409
        r3 = await client.post("http://127.0.0.1:8000/api/v1/auth/login",
                               json={"username": "alice3", "password": "secret123"})
        print("登录状态码:", r3.status_code)       # 200
        print("登录响应:", r3.json())

asyncio.run(main())
```

数据库验证(密码是哈希不是明文):
```sql
SELECT id, username, hashed_password FROM users;
-- 结果: $2b$12$... 开头,无明文
```

---

## 第六课:会话与消息模型 + 当前用户接口(2026-08-02)

### 成果
- `app/models/conversation.py` — 会话表(1 用户 → N 会话)
- `app/models/message.py` — 消息表(1 会话 → N 消息,级联删除)
- `app/api/deps.py` — 新增 `get_current_user` 依赖 + `CurrentUser` 别名
- `app/api/routers/users.py` — `GET /users/me` 当前用户接口
- `app/schemas/conversation.py` — 会话/消息请求响应模型
- `app/api/routers/conversations.py` — 6 个接口(创建/列表/详情/删除/发消息/看消息)
- `test_conversations.py` — 15 项全链路测试(含 3 项越权)

#### 会话模型(conversation.py)
```python
class Conversation(Base):
  __tablename__ = "conversations"
  id: Mapped[int] = mapped_column(primary_key=True, autoincrement=True)
  user_id: Mapped[int] = mapped_column(ForeignKey("users.id"), index=True, nullable=False)
  title: Mapped[str] = mapped_column(String(100), default="新对话", nullable=False)
  created_at: Mapped[datetime] = mapped_column(DateTime, server_default=func.now())
  updated_at: Mapped[datetime] = mapped_column(DateTime, server_default=func.now(), onupdate=func.now())
```

#### 消息模型(message.py)
```python
class Message(Base):
  __tablename__ = "messages"
  id: Mapped[int] = mapped_column(primary_key=True, autoincrement=True)
  conversation_id: Mapped[int] = mapped_column(
      ForeignKey("conversations.id", ondelete="CASCADE"), index=True, nullable=False)
  role: Mapped[str] = mapped_column(String(20), nullable=False)   # "user" / "assistant"
  content: Mapped[str] = mapped_column(Text, nullable=False)
  created_at: Mapped[datetime] = mapped_column(DateTime, server_default=func.now())
```

#### 依赖注入(get_current_user)
```python
oauth2_scheme = OAuth2PasswordBearer(tokenUrl="/api/v1/auth/login")

async def get_current_user(token: Annotated[str, Depends(oauth2_scheme)], db: DbDep) -> User:
  credentials_exception = HTTPException(
      status_code=401, detail="登录状态无效或已过期",
      headers={"WWW-Authenticate": "Bearer"})
  try:
    payload = decode_access_token(token)
    user_id = int(payload["sub"])
  except Exception:
    raise credentials_exception
  user = await db.get(User, user_id)
  if not user:
    raise credentials_exception
  return user

CurrentUser = Annotated[User, Depends(get_current_user)]
```

#### 归属校验模式(conversations.py)
```python
async def _get_owned_conversation(db: DbDep, conversation_id: int, user) -> Conversation:
  conv = await db.get(Conversation, conversation_id)
  if not conv or conv.user_id != user.id:
    raise HTTPException(status_code=404, detail="会话不存在")
  return conv
# 每个涉及具体会话的接口第一行都调它,校验不过自动 404
```

### 知识点
1. **一对多建模**:子表外键 `ForeignKey("users.id")` 指向父表主键;`index=True` 给外键列建索引(按用户查会话走索引)
2. **级联删除**:`ondelete="CASCADE"` 写在 ForeignKey 里——父行删除,子行自动删。验证要看 `SHOW CREATE TABLE`(DESC 看不到外键约束)
3. **两种默认值**:`default=` Python 侧(ORM 插入时生效,裸 SQL 不生效);`server_default=` 数据库侧(裸 SQL 也生效)。时间戳用 server_default;`onupdate=func.now()` 更新自动刷新
4. **SQLAlchemy 2.0 注解推断 nullable**:`Mapped[datetime]`(非 Optional)→ NOT NULL;想可空必须写 `Mapped[datetime | None]`
5. **依赖注入实战**:`OAuth2PasswordBearer` 从 `Authorization: Bearer <token>` 抠 token(tokenUrl 只给 Swagger 用);依赖函数可再依赖别的依赖,FastAPI 层层自动解析;失败统一 401 + `WWW-Authenticate: Bearer`
6. **db.get(模型, 主键)**:按主键查一行,查不到返回 None(等价 select + where id=)
7. **db.scalars 查多行** + `.all()` 取列表;`order_by(...desc())` 倒序(最新在前)
8. **归属校验**:资源属于当前用户才放行,否则 404 而不是 403(不暴露资源存在性,防枚举探测)
9. **信任边界**:role 由服务器写死 "user",客户端只传内容——防伪造"AI 消息"
10. **路径参数** `{conversation_id}` 自动解析成 int;`@router.post("")` 空路径 = 用 prefix 本身;204 删除无响应体,函数不 return

### 踩过的坑
1. **`Dserver_default` 拼错** → SAWarning + **静默忽略**:SQLAlchemy 把认不出的关键字当"方言参数",不报错只是警告 → created_at 没默认值。**Warning 不能无视**
2. **`onupdate="CASCADE"` 写错位置**:ondelete 必须写在 `ForeignKey(...)` 里;`mapped_column(onupdate=...)` 是"Python 侧更新默认值"(每次 UPDATE 把该列设为 'CASCADE' 字符串)→ 潜伏运行时炸弹
3. **await 丢失 → 越权漏洞(最危险)**:`if _get_owned_conversation(...)` 没 await → 协程对象永远为真 → 校验失效,能往别人的会话发消息/看别人的消息。**凡是调用 async 函数,先问"要不要 await"**
4. **`asyncio.run(main())` 缩进进函数体**:脚本只定义函数从不调用 → 零输出
5. **httpx 本地测试 502**:httpx 默认 `trust_env=True` 读系统代理 → 127.0.0.1 也被丢进代理 → 代理返回 502(uvicorn 从不返回 502,502 是代理/网关错误码)。修:`httpx.AsyncClient(trust_env=False)`
6. **`--reload` 默认监视整个目录**:保存测试脚本/IDE 触碰文件都会触发重启 → 撞上重启窗口 502。修:启动加 `--reload-dir app`
7. **Bearer 头少空格**:`f"Bearer{token}"` → 应为 `f"Bearer {token}"`;没空格 FastAPI 解析不出 token → 一律 401

### 验证
`test_conversations.py` 15 项全绿:注册 201×2、创建会话 201、发消息 201(role=user)、消息列表 200(1→2 条)、会话列表 200、详情 200、**越权三连 404**(B 用 A 的会话 id 访问/发消息/看消息)、删除 204、删后再查 404。

---

## Git 提交记录

| 提交 | 说明 |
|---|---|
| `db0dfd3` | feat: FastAPI 最小应用,健康检查接口 |
| `d6440b1` | feat: 项目分层结构 + pydantic-settings 配置管理 |
| `99a227a` | feat: SQLAlchemy 异步数据库连接(引擎+会话+依赖) |
| `1b52c3b` | feat: ORM 基类 + User 模型,建表到 MySQL;添加项目历程文档 |
| `191c644` | chore: 添加 .gitignore 和建表脚本 |
| `83f1c02` | feat: bcrypt 密码哈希 + JWT 签发与验证 |
| `928aec6` | feat: bcrypt 密码哈希与验证 |
| `5401bc7` | feat: 添加 JWT 签名算法配置 |
| `cae5cbc` | feat: 注册登录接口(bcrypt 密码哈希 + JWT 认证) |
| `a4fccda` | docs: 第五课认证接口学习回顾 |
| (待提交) | feat: 第六课 会话与消息模型 + 当前用户接口 + 会话CRUD |

---

## 常用命令速查

```powershell
# 启动服务
python -m uvicorn app.main:app --reload --reload-dir app   # --reload-dir 只监视 app 目录,测试脚本/IDE 不会触发重启

# 建表
python -m app.init_db

# 查 MySQL
docker exec ai-knowledge-mysql mysql -uapp -papp123456 ai_knowledge -e "SHOW TABLES;"
docker exec ai-knowledge-mysql mysql -uapp -papp123456 ai_knowledge -e "DESC users;"

# 看端口占用
Get-NetTCPConnection -LocalPort 8000 -State Listen

# git
git add <文件> && git commit -m "..." && git push
```

---

## 下一步计划

- [x] 第五课:认证接口(注册/登录)— 密码哈希(bcrypt)+ JWT
- [x] 第六课:会话与消息模型 + 获取当前用户接口(依赖注入实战)
- [ ] 第七课:文档上传与 RAG 链路(文件上传 → 切片 → Embedding → Qdrant 检索)
- [ ] 更多路由、测试、Docker 部署
